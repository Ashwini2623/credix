import 'package:flutter/material.dart';

const kAssetImagePath = 'assets/images';

SizedBox kSizedBoxHeight([double height16 = 16.0]) {
  return SizedBox(height: height16);
}

SizedBox kSizedBoxWidth([double width16 = 16.0]) {
  return SizedBox(
    width: width16,
  );
}

const kBlueColor = Colors.blueAccent;
const kRedColor = Colors.red;
const kGreyColor = Colors.grey;
const kWhiteColor = Colors.white;
const kDarkBlue = Color(0xff005b96);
